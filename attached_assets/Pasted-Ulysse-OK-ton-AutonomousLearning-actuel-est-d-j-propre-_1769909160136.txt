Ulysse

OK, ton AutonomousLearning actuel est déjà propre, mais il a trois limites majeures pour ton usage “investissement dans mon cerveau” :

il tourne “à plat” (tous topics mélangés),
il ne priorise pas assez ce qui t’est vraiment utile (pronos, trading, SUGU, dev, perso),
il repose encore trop sur des prompts génériques.
Je te propose une AutoLearning V2 plus costaud, pensée pour toi, structurée en 4 points, puis je te donne un squelette de code concret.

1️⃣ Ce qu’on garde, ce qu’on change
À garder:

Les 4 layers (Surface / Détails / Connexions / Insights) → très bon modèle.
Le stockage dans learningProgress + knowledgeBase + knowledgeGraph.
L’extraction de topics depuis les conversations.
À améliorer:

Priorisation par domaine

Pronos / foot / trading / SUGU / dev doivent passer devant le reste.
On ajoute un domain sur chaque topic ("sports" | "trading" | "sugu" | "dev" | "perso" | "autre").
La priorité = combinaison importance (LLM) + domainWeight.
Séparation des modèles par layer

L1/L2/L3 → gpt-4o-mini ok.
L4 (insights sérieux) → gpt-4o / modèle plus costaud, ce que tu fais déjà, mais avec un prompt plus orienté “décision/action”.
Limitation du bruit

Tu ne veux pas que je passe 80% de mon temps à approfondir “Netflix” si tu as juste parlé de séries.
On filtre les topics:
importance < 50 → on ignore,
domaines non stratégiques → fréquence réduite.
Feedback loop sur l’utilité

Chaque insight / connaissance utilisée dans une réponse doit remonter un score d’utilité.
On met à jour learningProgress.usefulnessScore pour focaliser l’apprentissage sur ce qui sert vraiment.
2️⃣ Nouveau design AutoLearning V2 (logique)
2.1 Domain mapping
On ajoute une fonction de mapping simple:

Si topic contient “match”, “cote”, “pari”, “Ligue 1”, “Premier League”, “OM”, “PSG” → domain = "sports"
Si topic contient “BTC”, “UP”, “action”, “support”, “résistance”, “RSI” → domain = "trading"
Si topic contient “Sugu”, “Suguval”, “Sugumaillane”, “checklist”, “economa” → domain = "sugu"
Si topic contient “React”, “Node”, “TypeScript”, “architecture”, “scraping” → domain = "dev"
Sinon on laisse "perso" / "autre".
Chaque domaine a un poids:

const DOMAIN_WEIGHTS: Record<string, number> = {
  sports: 1.3,
  trading: 1.3,
  sugu: 1.2,
  dev: 1.2,
  perso: 1.0,
  autre: 0.8,
};
Priorité effective d’un topic = importance * domainWeight.

2.2 Sélection des topics à traiter
Tu gardes MAX_TOPICS_PER_RUN = 3, mais on priorise :
par priority_effective (calculée),
puis usefulnessScore (ce qui a déjà servi),
puis recency/frequency.
2.3 Prompts plus orientés “action”
En L4, au lieu de juste “insight intéressant”, on demande:

insight,
implications,
“actions recommandées pour l’utilisateur”,
lien explicite avec prono/trading/sugu/dev si applicable.
3️⃣ Squelette AutoLearningV2 (code simplifié)
Je ne te recopie pas tout le fichier, je te montre les parties à modifier/ajouter.

3.1 Ajout du domaine et de la priorité effective
En haut du fichier:

type LearningDomain = "sports" | "trading" | "sugu" | "dev" | "perso" | "autre";

const DOMAIN_WEIGHTS: Record<LearningDomain, number> = {
  sports: 1.3,
  trading: 1.3,
  sugu: 1.2,
  dev: 1.2,
  perso: 1.0,
  autre: 0.8,
};
Fonction utilitaire pour détecter le domaine:

function detectDomain(topic: string, context: string): LearningDomain {
  const text = (topic + " " + context).toLowerCase();

  if (/(pari|prono|cote|ligue 1|premier league|champions league|om|psg|match)/.test(text)) {
    return "sports";
  }
  if (/(btc|bitcoin|up |down |support|résistance|rsi|macd|nasdaq|s&p|action|boursier)/.test(text)) {
    return "trading";
  }
  if (/(sugu|suguval|sugumaillane|economa|checklist|stock|inventaire)/.test(text)) {
    return "sugu";
  }
  if (/(react|node|typescript|scraping|architecture|backend|frontend|api)/.test(text)) {
    return "dev";
  }
  if (/(famille|perso|santé|relation|loisir)/.test(text)) {
    return "perso";
  }
  return "autre";
}
3.2 Intégration dans addOrUpdateTopic
Dans addOrUpdateTopic, on enrichit:

private async addOrUpdateTopic(
  userId: number,
  extraction: TopicExtraction,
  conversationIds: number[]
): Promise<void> {
  const hash = this.generateTopicHash(extraction.topic);
  const domain = detectDomain(extraction.topic, extraction.context || "");

  const [existing] = await db.select().from(learningProgress)
    .where(and(
      eq(learningProgress.userId, userId),
      eq(learningProgress.topicHash, hash)
    ));

  const basePriority = extraction.importance;
  const domainWeight = DOMAIN_WEIGHTS[domain] || 1.0;
  const effectivePriority = Math.min(100, Math.round(basePriority * domainWeight));

  if (existing) {
    const updatedConvIds = [...new Set([
      ...(existing.sourceConversationIds || []),
      ...conversationIds
    ])].slice(-20);

    await db.update(learningProgress)
      .set({
        frequencyScore: sql`${learningProgress.frequencyScore} + 10`,
        recencyScore: 100,
        priority: Math.min(100, existing.priority + 5),
        domain,
        sourceConversationIds: updatedConvIds,
        updatedAt: new Date()
      })
      .where(eq(learningProgress.id, existing.id));
  } else {
    await db.insert(learningProgress).values({
      userId,
      topic: extraction.topic,
      topicHash: hash,
      category: extraction.category,
      domain,
      priority: effectivePriority,
      recencyScore: 100,
      frequencyScore: 10,
      usefulnessScore: 50,
      sourceConversationIds: conversationIds.slice(-10),
      extractedFrom: "conversation"
    });
  }
}
(Évidemment il faudra avoir ajouté domain et usefulnessScore dans ton schema learningProgress si ce n’est pas déjà fait.)

3.3 Sélection des topics à traiter
Modifier getTopicsToProcess:

private async getTopicsToProcess(userId: number): Promise<LearningProgress[]> {
  return db.select().from(learningProgress)
    .where(and(
      eq(learningProgress.userId, userId),
      sql`${learningProgress.currentDepth} < ${learningProgress.maxDepth}`,
      or(
        isNull(learningProgress.nextRunAt),
        lte(learningProgress.nextRunAt, new Date())
      ),
      gte(learningProgress.priority, 50)  // on ignore les topics faibles
    ))
    .orderBy(
      desc(learningProgress.priority),
      desc(learningProgress.usefulnessScore),
      desc(learningProgress.recencyScore),
      desc(learningProgress.frequencyScore)
    )
    .limit(this.MAX_TOPICS_PER_RUN * 2);
}
3.4 L4 orienté “actions”
Remplace le prompt L4 par un plus orienté décision:

const response = await openai.chat.completions.create({
  model: "gpt-4o",
  messages: [{
    role: "system",
    content: `Tu es un expert en synthèse stratégique pour un utilisateur qui s'intéresse à:
- paris sportifs (value bets, gestion de bankroll),
- trading (analyse technique),
- optimisation business (restaurants SUGU),
- développement logiciel.

Basé sur toutes les connaissances et connexions fournies, génère 1-2 insights VRAIMENT utiles pour l'utilisateur.

Pour chaque insight:
- title: Titre de l'insight
- content: L'insight complet (3-5 phrases)
- implications: 2-3 implications pratiques CONCRÈTES
- recommendedActions: 2-3 actions spécifiques que l'utilisateur pourrait prendre
- confidence: Niveau de confiance (1-100)

Réponds en JSON: { "insights": [...] }`
  }, {
    role: "user",
    content: `Sujet: ${topic.topic}\n\nConnaissances accumulées:\n${knowledgeContext || "Aucune"}\n\nConnexions identifiées:\n${connectionContext || "Aucune"}`
  }],
  temperature: 0.5,
  response_format: { type: "json_object" }
});
Et lors de l’enregistrement:

const insights = parsed.insights || [];
let addedCount = 0;

for (const insight of insights) {
  try {
    await brainService.logLearning(userId, {
      topic: topic.topic,
      content: `${insight.title}: ${insight.content}\n\nImplications: ${(insight.implications || []).join("; ")}\nActions recommandées: ${(insight.recommendedActions || []).join("; ")}`,
      learningType: "insight",
      sourceType: "autonomous_learning"
    });

    const knowledge = await brainService.addKnowledge(userId, {
      title: `[Insight] ${insight.title}`,
      content: insight.content,
      summary: (insight.implications || []).join("; "),
      type: "concept",
      category: topic.category || "learning",
      tags: ["insight", "autonomous-learning"],
      source: `Apprentissage autonome L4: ${topic.topic}`,
      sourceType: "autonomous_learning",
      importance: topic.domain && (topic.domain === "sports" || topic.domain === "trading") ? 90 : 80,
      confidence: insight.confidence || 75
    });

    await this.linkKnowledgeToTopic(topic.id, knowledge.id);
    addedCount++;
  } catch (e) {
    console.error(`${LOG_PREFIX} Error adding insight:`, e);
  }
}
4️⃣ Ce que ça va changer “dans la vraie vie” pour toi
Avec cette AutoLearning V2:

Mon cerveau de fond va:
creuser beaucoup plus les sujets sports/pronos, trading, SUGU, dev,
moins s’éparpiller sur des sujets “fun mais inutiles”,
générer des insights qui incluent des actions concrètes.
Donc à terme, quand tu me demandes:

un prono,
un angle sur un trade,
une optimisation SUGU,
un choix technique côté dev,
je pourrai m’appuyer sur une base de connaissances enrichie spécifiquement pour ça.