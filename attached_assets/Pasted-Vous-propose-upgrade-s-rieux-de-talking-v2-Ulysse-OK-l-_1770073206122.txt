Vous

propose upgrade sérieux de /talking-v2

Ulysse

OK, là on parle. Je te fais un vrai plan d’upgrade “sérieux” pour /talking-v2, orienté concret produit + technique.

Je te le découpe en 5 gros axes, chacun actionnable.

1. Cerveau spécialisé “Voice Mode” (le cœur du problème)
Idée
Quand la requête vient de /talking-v2, tu ne veux pas le même comportement que le chat texte.
Il faut un profil de décision spécial côté backend.

Upgrade
a) Flag contextuel côté backend

Dans useRealtimeVoice / /api/realtime (ou équivalent), tu tagues chaque message avec:

{
  "origin": "voice",
  "channel": "talking-v2",
  "mode": "continuous"
}
Et côté orchestrateur:

const isVoice = metadata.origin === "voice" && metadata.channel === "talking-v2";
b) Routing de logique “voice-first”

Si isVoice === true :

Activer des “pipelines” spécialisés :
handleFootballQuery
handleSuguvalQuery
handleWeatherQuery
handleGenericConv
En pseudo-code:

if (isVoice) {
  const intent = detectIntent(userText); // LLM + heuristiques
  switch (intent.domain) {
    case "football":
      return answerFromFootballStack(userText);
    case "restaurants":
      return answerFromSuguStack(userText);
    default:
      return genericLLMReply(userText, { style: "oral" });
  }
}
Objectif: ne JAMAIS retomber sur le vieux réflexe “je n’ai pas accès au temps réel” tant qu’on n’a pas interrogé nos propres stacks.

2. Intégration FOOT sérieuse pour la voix
Idée
Pour tout ce qui est foot, /talking-v2 doit être branché en dur sur footdatas + cache Djedou Pronos + MatchEnDirect.

Upgrade
a) Intent + slot filling minimal

Tu peux faire un petit detectFootballIntent():

Exemples:
“score du dernier match de l’OM”
“prochain match de Marseille”
“qui est 3e en Ligue 1”
“meilleurs buteurs de Liga”
Tu passes le texte à un mini-LMM ou un prompt interne:

type FootballIntent =
  | { type: "last_match"; team: string }
  | { type: "next_match"; team: string }
  | { type: "ranking"; league: string }
  | { type: "topscorers"; league: string }
  | null;
b) Pipeline concret

async function answerFromFootballStack(userText: string) {
  const intent = detectFootballIntent(userText);
  if (!intent) return null; // laisse le LLM répondre normalement

  switch (intent.type) {
    case "last_match":
      return answerLastMatch(intent.team);
    case "next_match":
      return answerNextMatch(intent.team);
    case "ranking":
      return answerRanking(intent.league);
    case "topscorers":
      return answerTopscorers(intent.league);
  }
}
c) Exemple: dernier match OM

async function answerLastMatch(teamName: string) {
  // 1. DB footdatas
  const match = await footdatas.getLastMatchForTeam(teamName);
  if (match) return formatLastMatchVoice(match);

  // 2. Cache prédictions / cache matches
  const cached = await sportsCache.getLastMatch(teamName);
  if (cached) return formatLastMatchVoice(cached);

  // 3. Fallback matchendirect
  const live = await queryMatchEndirect(teamName);
  if (live) return formatLastMatchVoice(live);

  return "Je ne trouve pas de dernier match fiable pour " + teamName +
         ". Soit ils n'ont pas joué récemment, soit mes données sont incomplètes.";
}
d) Style de réponse voix

Adapter le wording:

“Le dernier match de l’OM, c’était contre Lens, victoire deux à un à domicile. Buteurs : Greenwood et Aubameyang.”

Court, oral, sans parenthèses à rallonge.

3. Voice UX : flow, interruptions et confirmations
Ta base est déjà très bonne, mais on peut durcir.

a) Auto-confirmation des actions “critiques”
Exemples: envoyer email, commandes Suguval, domotique.

Pour une requête ambiguë, le backend renvoie un “plan d’action” + phrase de confirmation:

{
  "type": "confirm_action",
  "action": "send_email_suguval",
  "summary": "envoyer le récap des courses Suguval de ce soir à ton email habituel"
}
Côté front, quand type === confirm_action, tu passes en:

“Je peux envoyer le mail des courses de Suguval pour ce soir. Tu veux que je l’envoie ?”

Et tu attends un “oui / non” vocal.
En cas de “oui” → tu rappelles le backend avec l’actionId.

b) Politique d’interruption plus smart
Tu as déjà l’auto-interrupt sur niveau mic. On peut ajouter une couche côté modèle:

Quand interruption détectée:
voice.cancel()
côté backend: taguer la réponse comme “interrompue”, ne pas continuer la phrase suivante dans le prompt.
Option: envoyer au backend un event user_interrupt pour qu’il adapte son style et raccourcisse les réponses suivantes.

4. Personnalisation par utilisateur (Maurice / Kelly / Lenny / Micky)
Tu as déjà TALKING_USERS + personas. On peut en faire quelque chose de plus intelligent.

a) Profils vocaux / habitudes
Mémoriser par user:

preferred_conversation_mode (continuous / push-to-talk)
default_domain_bias (Maurice: foot + Sugu, Kelly: planning + famille, etc.)
answer_length (Maurice: court & direct, Kelly: un peu plus explicatif par défaut)
Côté backend, tu peux injecter ça dans le prompt:

{
  "user": "maurice",
  "persona": "ulysse",
  "preferences": {
    "style": "direct",
    "domains_priority": ["football", "restaurants", "productivité"]
  }
}
Et adapter les réponses.

b) Voice security feedback
Tu as déjà onSpeakerVerified / onSpeakerRejected.
Tu peux:

Logguer les rejets avec un score,
Si 3 rejets consécutifs → passer en mode “degraded + verrouillé” pour ce device.
5. Observabilité & debug pour /talking-v2
Si tu veux que ça scale propre, il te faut de la télémétrie dédiée voix.

a) Log structuré par tour de dialogue
Pour chaque message:

{
  "channel": "talking-v2",
  "user": "maurice",
  "text": "score du dernier match de l'OM",
  "intent": "football.last_match",
  "data_sources_used": ["footdatas", "sports_cache"],
  "latency_ms": 420,
  "success": true
}
Tu peux le pousser dans une table voice_sessions ou dans un log type Logflare / Loki.

b) Dashboard simple “Voice Health”
taux de réussite par intent (football, sugu, générique)
latence moyenne
nombre d’erreurs / 24h
ratio interruptions / réponses complètes
Bonus: quick wins bien concrets
Empêcher les réponses “je n’ai pas accès au temps réel” en voice

Ajout d’une règle dans le prompt “voice”:
Interdit de dire ça si query_sports_data / footdatas / cache ne sont pas interrogés.
Réponses foot pré-formatées

Helper TS formatLastMatchVoice, formatRankingVoice, etc., pour des réponses cohérentes et rapides.
Commandes système vocales

Genre “change de mode”, “mets-toi en sourdine”, “raccroche” → map direct aux handlers frontend (handleToggleMode, handleEndCall, etc.) via un canal system_command depuis backend.
Si tu veux, prochaine étape je peux:

Te rédiger un mini-RFC markdown “/talking-v2 v3 – Voice Brain Spec” que tu peux coller dans ton repo (doc technique),
Ou te sortir directement le pseudo-code backend d’un answerFromFootballStack complet, plug & play avec footdatasService + cache sports.
Vous

Détecteur de Silence PRO V2 installé ! 🎯 Améliorations de qualité professionnelle : 1. Calibration automatique du bruit de fond 30 échantillons (~500ms) au démarrage Calcule la moyenne + écart-type du bruit ambiant Seuil adaptatif = bruit × 2.5 (dynamique selon l'environnement) 2. Mesure RMS (Root Mean Square) Calcul professionnel du niveau audio Plus précis que la simple moyenne FFT à 512 bins pour meilleure résolution 3. Hystérésis anti-faux positifs 5 frames consécutives pour confirmer la parole 8 frames consécutives pour confirmer le silence Évite les déclenchements parasites (bruits de fond, clics) 4. Validation de la parole Durée minimum de parole: 300ms (pas de faux positifs sur "hm", "uh") Silence de 1.5s après parole = fin détectée Statistiques complètes (peak level, avg level, durée) Flux : Calibration (~500ms) - mesure le bruit de fond Écoute active - seuil adaptatif basé sur l'environnement Confirmation parole - 5 frames consécutives > seuil Détection fin - 1.5s silence après parole confirmée → traitement

Ulysse

Excellent, là on commence à jouer dans la cour des grands.

Je récap’ et je te dis comment le brancher intelligemment avec le reste de /talking-v2.

Ce que ton “Détecteur de Silence PRO V2” apporte
Calibration auto bruit de fond (~500 ms)

30 échantillons
moyenne + écart-type
seuil adaptatif = bruit × 2.5
→ Donc plus de seuil fixe débile qui flingue l’expérience selon la pièce.
Mesure RMS + FFT 512 bins

RMS = vrai niveau perçu (pro)
FFT plus fine = meilleure estimation de l’énergie utile
→ Idéal pour différencier voix vs. bruit large bande / ventilateur.
Hystérésis anti-faux positifs

parole: 5 frames consécutives au-dessus du seuil
silence: 8 frames consécutives en-dessous → Exit les “pics” isolés qui font croire que tu parles.
Validation parole / fin de phrase

min 300 ms de parole → pas déclenché sur un “hm”
1.5 s de silence = fin de tour
→ Parfait pour délimiter un tour de parole propre pour le backend.
Comment l’exploiter dans /talking-v2 (le vrai upgrade)
1. Tu remplaces la logique “micLevel > 0.15” par ton détecteur pro
Actuellement tu as un truc comme:

if (conversationMode === "continuous" && voice.isSpeaking && isInCall && !isMuted && micMonitorActive && micLevel > 0.15) {
  // auto-interrupt
}
Tu peux exposer ton détecteur comme un petit hook ou service:

type SpeechState = "idle" | "speaking" | "silence" | "transition";

const { speechState, rmsLevel } = useProSilenceDetector({ stream });
Et remplacer la condition par:

if (
  conversationMode === "continuous" &&
  voice.isSpeaking &&
  isInCall &&
  !isMuted &&
  speechState === "speaking"
) {
  // auto-interrupt
}
→ Tu ne relies plus l’auto-interrupt à un “niveau brut” mais à un état de parole validé.

2. Détection de fin de tour pour envoyer au backend
Use-case top :

Mode continu: tu laisses l’utilisateur parler,
Tu détectes fin de parole (1.5 s de silence après parole confirmée),
À ce moment précis, tu figes le transcript et tu déclenches l’envoi.
Pseudo-code dans /talking-v2 :

useEffect(() => {
  if (!isInCall || isMuted) return;
  if (speechState === "silence" && lastStateRef === "speaking") {
    // Fin de phrase détectée
    voice.stopListening(); // ou marquer fin de tour
    // tu laisses useRealtimeVoice pousser la requête
  }
  lastStateRef = speechState;
}, [speechState, isInCall, isMuted]);
Résultat:

Plus besoin de couper à la main,
Tu peux gérer des tours de parole propres, comme un vrai intercom.
3. Stats audio → debug & UX
Tu dis que ton détecteur sort:

peak level
average level
durée de parole
Tu peux:

logger ça côté backend (par tour de dialogue)
afficher un mini debug panel caché (option “Dev mode”) sur /talking-v2:
“Dernier tour: 1.8s, niveau moyen: -18 dB, peak: -8 dB”
C’est gold pour:

diagnostiquer les micros pourris,
voir si le seuil adaptatif est trop agressif dans certaines pièces.
4. Interop avec auto-resume listening
Tu as déjà un useEffect qui fait:

if (wasIdle && isInCall && conversationMode === "continuous" && !isMuted && !isResumingListeningRef.current) {
  // auto-resume listening après TTS
}
Tu peux le rendre encore plus smart:

ne relancer l’écoute que si le détecteur dit “silence stable” (aucun bruit parasite).
if (
  wasIdle &&
  isInCall &&
  conversationMode === "continuous" &&
  !isMuted &&
  !isResumingListeningRef.current &&
  speechState === "silence"
) {
  // relance listening
}
Ça évite de rouvrir le micro en plein bruit de fond.

En résumé
Oui, ton “Détecteur de Silence PRO V2” est exactement ce qu’il fallait pour passer de “gadget voice” à UX pro stable.
La clé maintenant, c’est:
Exposer ce détecteur comme un état propre (speechState, rmsLevel, stats).
Brancher cet état dans:
auto-interrupt,
détection fin de tour,
auto-resume listening,
quelques logs pour le debug.
Si tu veux, je peux te sortir un pseudo-hook useProSilenceDetector(stream) complet (TypeScript) que tu peux coller dans ton repo et brancher direct dans /talking-v2.