Ulysse

Ce module est très solide. Tu viens de te coder un vrai Moteur de Recherche Autonome Ulysse.

En clair, tu as déjà :

Détection des lacunes (analyzeGaps) avec seuils agressifs:
pas assez de faits vérifiés,
pas assez de sources,
fiabilité moyenne faible,
dataGaps explicites, etc.
Apprentissage par requête (scopé par user):
learnFromResult mémorise quelles reformulations marchent / échouent,
getLearnedStrategies / getFailedStrategies réutilisent ça pour les requêtes futures.
Reformulation intelligente:
templates + stratégies apprises,
évite les patterns qui ont déjà échoué.
Deep dive automatique:
executeDeepResearch relance jusqu’à 4 recherches complémentaires,
fusionne les résultats, croise les faits, compte le nombre de sources,
calcule un researchDepth et un set de crossReferencedFacts.
Enrichissement par mémoire:
va piocher dans webSearchMemory + marsSearchHistory pour ajouter du contexte déjà appris.
Interface haut niveau:
searchWithAutonomy(userId, query) → te renvoie soit le MARS brut, soit un EnrichedSearchResult + un formattedForAI déjà prêt à injecter dans mes prompts.
Conclusion:
Avec Verified Scraper + Ulysse Translation Core + ce autonomousResearchService, tu as posé un niveau “Super Ulysse” sur:

scraping,
traduction,
recherche web,
et fiabilité des données.
Tu as maintenant une stack cohérente, robuste et orientée “zéro hallucination” pour tout ce qui est data / web / foot / cotes.

Vous

ameliore le pour devenir SUPER ULYSSE

Ulysse

Tu viens de te coder un mini-Moteur de recherche autonome.
On va le pousser en SUPER ULYSSE sans tout casser, juste en ajoutant des étages intelligents.

Je vais te proposer des améliorations concrètes sur 4 axes, avec des snippets là où ça compte.

1️⃣ Coupler encore plus fort MARS + Verified Scraper
Actuellement, autonomousResearchService exploite uniquement MARS.
Pour tous les sujets “structurels” (classements, cotes, stats, tableaux), tu peux:

utiliser MARS pour trouver les bonnes URLs,
puis passer ces URLs par ton Verified Scraper (double-scrape validé),
et intégrer ces données comme “hard facts” dans crossReferencedFacts.
a) Détecter les requêtes qui méritent un scrape structuré
Dans searchWithAutonomy, après initialResult, tu peux détecter les queries “football/cotes:

const isStructuredSportsQuery = /classement|ranking|standings|buteur|scorer|cotes?|odds?|score|match/i.test(
  query.toLowerCase()
);
b) Extraire des URLs candidates via MARS pour scraping structuré
Dans executeDeepResearch, avant la boucle de reformulations, tu peux:

import { autoVerifiedScrape } from "./verifiedScraperService";

async function enrichWithVerifiedScrapes(
  userId: number,
  marsResult: MARSSearchResult
) {
  const urls = marsResult.orchestratorResponse.results
    .map(r => r.url)
    .filter(u => !!u)
    .slice(0, 3); // max 3 pour limiter la charge

  const verifiedData: any[] = [];

  for (const url of urls) {
    try {
      const { type, result } = await autoVerifiedScrape(url, undefined, { maxAttempts: 2 });
      if (result.ok && result.verified && result.data) {
        verifiedData.push({ type, url, data: result.data });
      }
    } catch (e) {
      console.warn("[AutonomousResearch] Verified scrape failed for", url);
    }
  }

  return verifiedData;
}
Ensuite, dans executeDeepResearch, tu peux appeler ça une fois (avant ou après les reformulations), et injecter ces faits dans allFacts avec une confiance maximale (multi-scrapes + multi-sources).

2️⃣ Exploiter Ulysse Translation Core quand les résultats ne sont pas en FR
Pour les recherches internationales, tu peux:

détecter la langue des snippets MARS,
traduire ce qui n’est pas en français via translationService,
stocker les facts en FR tout en gardant la VO en metadata.
a) Détection + traduction légère des snippets (optionnel mais puissant)
Dans executeDeepResearch, pendant que tu merges les facts, tu peux faire:

import { translationService } from "./translationService";

async function normalizeFactLanguage(fact: string): Promise<string> {
  // Ici tu peux stocker un petit cache local par run pour éviter de retraduire le même fact
  const res = await translationService.translate({
    text: fact,
    targetLang: "fr",
    sourceLang: "auto",
    domain: "general",
    tone: "neutral"
  });
  return res.translated || fact;
}
Et au moment d’ajouter dans allFacts:

const normalized = await normalizeFactLanguage(fact.content);
const key = normalized.toLowerCase().substring(0, 100);

if (allFacts.has(key)) {
  // merge...
} else {
  allFacts.set(key, {
    fact: normalized,
    sources: new Set(fact.sources),
    confidence: fact.confidence
  });
}
Résultat: même si les sources sont en anglais / italien, tes crossReferencedFacts sont déjà en français, alignées avec ton usage principal.

3️⃣ Intégrer directement la mémoire “VERIFIED” dans la recherche autonome
Tu as déjà enrichFromMemory qui va chercher dans webSearchMemory + marsSearchHistory.
Tu peux maintenant:

exploiter aussi memoryService.getVerifiedMemories
pour injecter les faits vérifiés de ton verified scraper ou d’autres pipelines.
a) Enrichir avec mémoires VERIFIED
Ajoute une fonction:

import { memoryService } from "./memory";

async function enrichFromVerifiedMemories(userId: number, query: string): Promise<string[]> {
  const memories = await memoryService.getVerifiedMemories(userId);
  const keywords = query.toLowerCase().split(/\s+/).filter(w => w.length > 3);

  const enrichments: string[] = [];

  for (const mem of memories) {
    const text = `${mem.key} ${mem.value}`.toLowerCase();
    const overlap = keywords.filter(k => text.includes(k)).length;
    if (overlap >= 1) {
      enrichments.push(`[MEMORY_VERIFIED] ${mem.value}`);
    }
  }

  return Array.from(new Set(enrichments)).slice(0, 5);
}
Puis dans executeDeepResearch:

const memoryEnrichment = await this.enrichFromMemory(userId, query);
const verifiedEnrichment = await enrichFromVerifiedMemories(userId, query);

const allEnrichment = [
  ...memoryEnrichment,
  ...verifiedEnrichment
];
Et tu renvoies allEnrichment au lieu de memoryEnrichment seul.

4️⃣ Niveau “Super Ulysse” : Prioriser les faits vérifiés et exposer clairement le niveau de confiance
Aujourd’hui tu as crossReferencedFacts avec sourceCount + confidence.
Tu peux rendre ça encore plus utile pour moi (l’IA) en:

séparant clairement:
FAITS VÉRIFIÉS (sourceCount ≥ 3),
FAITS PROBABLES (sourceCount = 2),
FAITS À PRENDRE AVEC DES PINCETTES (1 seule source),
et en ajoutant un bloc explicite dans formattedForAI qui me rappelle mes limites.
Tu le fais déjà partiellement; on peut durcir la règle.

Dans formatEnrichedResultsForAI tu peux renforcer:

const verifiedFacts = enriched.crossReferencedFacts.filter(f => f.confidence === "verified");
const probableFacts = enriched.crossReferencedFacts.filter(f => f.confidence === "probable");
const weakFacts = enriched.crossReferencedFacts.filter(f => f.confidence === "unverified");

if (weakFacts.length > 0) {
  formatted += `**FAITS FAIBLES (1 seule source - À TRAITER AVEC PRUDENCE):**\n`;
  for (const fact of weakFacts.slice(0, 5)) {
    formatted += `[WEAK] ${fact.fact} [${fact.sourceCount} source]\n`;
  }
  formatted += `\n`;
}

formatted += `\n> Règle d'utilisation: base-toi d'abord sur les FAITS VÉRIFIÉS, puis éventuellement sur les PROBABLES. Les WEAK ne doivent JAMAIS être présentés comme des certitudes.\n`;
Avec ça, quand ce bloc est injecté dans mon contexte, je sais clairement:

ce que j’ai le droit d’affirmer fort,
ce que je dois nuancer,
ce que je dois présenter comme “à confirmer”.
Comment avancer concrètement
Pour passer ton moteur en “Super Ulysse” real life, je te suggère cet ordre:

Ajouter enrichWithVerifiedScrapes dans executeDeepResearch pour les queries sports/cotes et injecter ces données dans allFacts.
Brancher translationService dans la normalisation des facts pour que les faits multi-langues arrivent déjà en FR.
Ajouter enrichFromVerifiedMemories et fusionner avec memoryEnrichment.
Durcir le formatage des faits dans formatEnrichedResultsForAI (bloc WEAK + règle d’utilisation).