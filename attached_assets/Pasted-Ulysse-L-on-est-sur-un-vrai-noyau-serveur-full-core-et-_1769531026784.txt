Ulysse

Là on est sur un vrai noyau serveur “full core”, et ton fichier est déjà très solide.

Je te fais une review courte, orientée améliorations possibles, pas du blabla.

1️⃣ Ce qui est déjà excellent
Sécurité:

setupSecurityMiddleware(app);
redirection .replit.app → domaine custom, avec exceptions propres (/talking, /ws/voice).
PWA manifest dynamique:

route /manifest.json qui sert différents manifests selon le referer (/courses/suguval, /talking, /alfred).
Logs HTTP:

middleware qui mesure la durée + log JSON pour /api → super utile pour diagnostiquer.
WebSockets unifiés:

setupRealtimeVoice, setupRealtimeSync, setupScreenMonitorWs.
httpServer.on("upgrade", ...) avec router /ws/voice, /ws/sync, /ws/screen + forward aux listeners existants (Vite HMR) → très propre.
Bootstrap non-bloquant:

ensureApprovedUsers() après listen.
test AgentMail non bloquant.
jobScheduler.start() après listen.
startSpeakerService() après listen.
Speaker Service Python:

spawn process avec stdout/stderr loggés.
restart auto après crash (code !== 0).
shutdown propre sur process.on("exit").
Franchement, pour du “core Ulysse”, c’est déjà très bien.

2️⃣ Améliorations ciblées (sans tout casser)
A. Gestion des erreurs globales
Tu as un middleware:

app.use((err: any, _req, res, _next) => {
  const status = err.status || err.statusCode || 500;
  const message = err.message || "Internal Server Error";

  res.status(status).json({ message });
  throw err;
});
Le throw err après avoir répondu peut être violent en prod (ça peut faire remonter des erreurs non catchées).

Je te conseille plutôt:

log + pas de throw (tu laisses le process vivant),
ou si tu veux un “crash on fatal”, tu gères ça en haut, pas ici.
Exemple:

app.use((err: any, _req: Request, res: Response, _next: NextFunction) => {
  const status = err.status || err.statusCode || 500;
  const message = err.message || "Internal Server Error";

  console.error("[ExpressError]", status, message, err.stack || err);
  if (!res.headersSent) {
    res.status(status).json({ message });
  }
});
Tu gardes le process vivant et tu évites les doubles-throws.

B. Logs API: les réponses complètes peuvent devenir lourdes
Actuellement tu log:

if (capturedJsonResponse) {
  logLine += ` :: ${JSON.stringify(capturedJsonResponse)}`;
}
Pour certaines routes (MARS, scraping, fichiers, etc.), ça peut être très volumineux.

Tu peux limiter :

soit à une taille max,
soit logguer uniquement pour les erreurs / status ≥ 400.
Exemple:

if (capturedJsonResponse) {
  const jsonStr = JSON.stringify(capturedJsonResponse);
  const truncated = jsonStr.length > 500 ? jsonStr.slice(0, 500) + "...(truncated)" : jsonStr;
  logLine += ` :: ${truncated}`;
}
Ou :

if (res.statusCode >= 400 && capturedJsonResponse) {
  // log complet uniquement en cas d’erreur
}
C. Speaker Service : health-check + backoff
Tu redémarres le service Python 5s après un crash, ce qui est bien.
Tu peux ajouter un check “disponible / pas dispo” côté Core (tu l’as déjà avec isSpeakerServiceAvailable()), mais pour le superviseur:

tu peux limiter les restart en cas de crash en boucle (genre max N restart en M minutes), pour éviter un spam de logs si le script est complètement cassé.
Ce n’est pas vital, mais ça peut éviter du bruit si un jour tu casses speaker_service.py.

D. Hook de shutdown plus large
Tu tues le speaker service sur process.on("exit").
Tu peux aussi ajouter SIGINT / SIGTERM pour un shutdown propre (utile hors Replit).

Exemple:

const shutdown = () => {
  console.log("[Shutdown] Cleaning up before exit...");
  if (speakerServiceProcess) {
    speakerServiceProcess.kill();
  }
  httpServer.close(() => {
    process.exit(0);
  });
};

process.on("SIGINT", shutdown);
process.on("SIGTERM", shutdown);
3️⃣ En résumé
Pour ce que tu veux – un Core Ulysse privé, robuste, avec:

sécurité,
websockets unifiés,
speaker service supervisé,
bootstrap smart,
logs corrects,
ce fichier est déjà à la hauteur.

Les améliorations que je te propose:

adoucir la gestion d’erreur Express (pas de throw err),
limiter la taille des logs JSON d’API,
éventuellement ajouter un peu de logique de backoff sur le speaker service,
et un shutdown propre pour quand tu ne seras plus sur Replit.
Rien de structurel à changer, juste du polish “pro”.

Vous

fais moi un fichier complet robuste et max

Ulysse

OK, on te fait un server.ts / index.ts vraiment “Core Ulysse MAX” :
même logique que ton code actuel, mais:

meilleure gestion d’erreurs,
logs API maîtrisés,
shutdown propre,
supervision speaker service un peu plus safe.
Je pars de ton code et je l’upgrade, pas de refactor sauvage.

import express, { type Request, Response, NextFunction } from "express";
import cookieParser from "cookie-parser";
import { registerRoutes } from "./routes";
import { serveStatic } from "./static";
import { createServer } from "http";
import { ensureApprovedUsers } from "./services/userBootstrap";
import { jobScheduler } from "./services/scheduledJobs";
import { setupRealtimeVoice, handleVoiceUpgrade } from "./services/realtimeVoice";
import { setupRealtimeSync, handleSyncUpgrade } from "./services/realtimeSync";
import { setupScreenMonitorWs, handleScreenUpgrade } from "./services/screenMonitorWs";
import { setupSecurityMiddleware } from "./middleware/security";
import { spawn, ChildProcess } from "child_process";
import path from "path";
import fs from "fs";

const app = express();
const httpServer = createServer(app);

// ---------- SECURITY ----------
setupSecurityMiddleware(app);

// ---------- MIDDLEWARES ----------
app.use(cookieParser());

// Dynamic manifest.json based on Referer for iOS PWA support
app.get("/manifest.json", (req, res, next) => {
  const referer = req.get("referer") || "";

  let manifestFile = "manifest.json";
  if (referer.includes("/courses/suguval")) {
    manifestFile = "manifest-suguval.json";
  } else if (referer.includes("/talking")) {
    manifestFile = "manifest-talking.json";
  } else if (referer.includes("/alfred")) {
    manifestFile = "manifest-alfred.json";
  }

  const manifestPath = path.join(process.cwd(), "client", "public", manifestFile);
  if (fs.existsSync(manifestPath)) {
    res.setHeader("Content-Type", "application/manifest+json");
    return res.sendFile(manifestPath);
  }
  next();
});

// Redirect .replit.app to custom domain in production (except /talking + /ws/voice)
app.use((req, res, next) => {
  const host = req.get("host") || "";
  const pathName = req.path;

  // /talking and /ws/voice are NEVER redirected - must work on any domain
  if (pathName.startsWith("/talking") || pathName.startsWith("/ws/voice")) {
    return next();
  }

  // Only redirect .replit.app to custom domain, never redirect between www/non-www
  if (process.env.NODE_ENV === "production" && host.includes(".replit.app")) {
    const customDomain = "ulysseproject.org";
    return res.redirect(301, `https://${customDomain}${req.originalUrl}`);
  }
  next();
});

declare module "http" {
  interface IncomingMessage {
    rawBody: unknown;
  }
}

app.use(
  express.json({
    limit: "10mb", // Allow large payloads for base64 images (vision API)
    verify: (req, _res, buf) => {
      req.rawBody = buf;
    },
  }),
);

app.use(express.urlencoded({ extended: false }));

// ---------- LOGGING ----------
export function log(message: string, source = "express") {
  const formattedTime = new Date().toLocaleTimeString("en-US", {
    hour: "numeric",
    minute: "2-digit",
    second: "2-digit",
    hour12: true,
  });
  console.log(`${formattedTime} [${source}] ${message}`);
}

// API logging middleware (with truncated JSON body)
app.use((req, res, next) => {
  const start = Date.now();
  const pathName = req.path;
  let capturedJsonResponse: Record<string, any> | undefined = undefined;

  const originalResJson = res.json;
  res.json = function (bodyJson, ...args) {
    capturedJsonResponse = bodyJson;
    return originalResJson.apply(res, [bodyJson, ...args]);
  };

  res.on("finish", () => {
    const duration = Date.now() - start;
    if (pathName.startsWith("/api")) {
      let logLine = `${req.method} ${pathName} ${res.statusCode} in ${duration}ms`;

      if (capturedJsonResponse && res.statusCode >= 400) {
        // Log complet uniquement en cas d'erreur pour éviter les gros dumps
        const jsonStr = JSON.stringify(capturedJsonResponse);
        const truncated =
          jsonStr.length > 500 ? jsonStr.slice(0, 500) + "...(truncated)" : jsonStr;
        logLine += ` :: ${truncated}`;
      }

      log(logLine);
    }
  });

  next();
});

// ---------- SPEAKER RECOGNITION SERVICE SUPERVISOR ----------
let speakerServiceProcess: ChildProcess | null = null;
let speakerRestartAttempts = 0;
const SPEAKER_MAX_RESTARTS = 5;
const SPEAKER_RESTART_WINDOW_MS = 10 * 60 * 1000; // 10 min
let speakerFirstCrashTime: number | null = null;

function resetSpeakerRestartWindow() {
  speakerRestartAttempts = 0;
  speakerFirstCrashTime = null;
}

function startSpeakerService() {
  if (speakerServiceProcess) {
    console.log("[Speaker] Service already running");
    return;
  }

  if (speakerRestartAttempts >= SPEAKER_MAX_RESTARTS) {
    const now = Date.now();
    if (speakerFirstCrashTime && now - speakerFirstCrashTime < SPEAKER_RESTART_WINDOW_MS) {
      console.error(
        "[Speaker] Too many restart attempts in short time. Not restarting automatically.",
      );
      return;
    } else {
      resetSpeakerRestartWindow();
    }
  }

  try {
    const pythonPath = process.env.PYTHON_PATH || "python3";
    speakerServiceProcess = spawn(pythonPath, ["speaker_recognition/speaker_service.py"], {
      env: { ...process.env, SPEAKER_PORT: "5001" },
      stdio: ["ignore", "pipe", "pipe"],
    });

    speakerServiceProcess.stdout?.on("data", (data) => {
      console.log(`[Speaker] ${data.toString().trim()}`);
    });

    speakerServiceProcess.stderr?.on("data", (data) => {
      console.error(`[Speaker] ${data.toString().trim()}`);
    });

    speakerServiceProcess.on("close", (code) => {
      console.log(`[Speaker] Service exited with code ${code}`);
      speakerServiceProcess = null;

      const now = Date.now();
      if (speakerFirstCrashTime === null) {
        speakerFirstCrashTime = now;
      }
      speakerRestartAttempts++;

      // Restart after 5 seconds if it crashes with non-zero code, with backoff protection
      if (code !== 0 && speakerRestartAttempts <= SPEAKER_MAX_RESTARTS) {
        setTimeout(startSpeakerService, 5000);
      } else if (speakerRestartAttempts > SPEAKER_MAX_RESTARTS) {
        console.error(
          "[Speaker] Max restart attempts reached. Manual intervention required.",
        );
      }
    });

    speakerServiceProcess.on("error", (err) => {
      console.error("[Speaker] Failed to start service:", err.message);
      speakerServiceProcess = null;
    });

    console.log("[Speaker] Recognition service starting on port 5001");
  } catch (error: any) {
    console.error("[Speaker] Failed to start service:", error.message);
    speakerServiceProcess = null;
  }
}

// ---------- CLEAN SHUTDOWN ----------
function shutdown() {
  console.log("[Shutdown] Cleaning up before exit...");
  if (speakerServiceProcess) {
    try {
      speakerServiceProcess.kill();
    } catch (e) {
      console.error("[Shutdown] Error killing speaker service:", (e as Error).message);
    }
  }
  httpServer.close(() => {
    process.exit(0);
  });
}

process.on("SIGINT", shutdown);
process.on("SIGTERM", shutdown);
process.on("exit", () => {
  if (speakerServiceProcess) {
    speakerServiceProcess.kill();
  }
});

// ---------- ASYNC INIT FOR HEAVY SERVICES ----------
async function initializeServices() {
  try {
    await ensureApprovedUsers();
    log("Approved users initialized", "startup");
  } catch (error: any) {
    console.error("[Startup] User bootstrap error:", error.message);
  }

  // Test AgentMail connectivity (non-blocking)
  try {
    const { agentMailService } = await import("./services/agentMailService");
    const connectivityResult = await agentMailService.testConnectivity();
    if (connectivityResult.success) {
      console.log(
        `[Startup] AgentMail connectivity OK: ${connectivityResult.inboxes.join(", ")}`,
      );
    } else {
      console.warn(
        `[Startup] AgentMail connectivity FAILED: ${connectivityResult.error}`,
      );
    }
  } catch (error: any) {
    console.error("[Startup] AgentMail test error:", error.message);
  }
}

// ---------- MAIN BOOTSTRAP ----------
(async () => {
  // 1) Initialize WebSocket servers FIRST (lightweight, no heavy I/O)
  setupRealtimeVoice();
  setupRealtimeSync();
  setupScreenMonitorWs();

  // 2) Register routes (must be done before listen)
  await registerRoutes(httpServer, app);

  // 3) Global error handler (no throw here, on log propre + réponse HTTP)
  app.use