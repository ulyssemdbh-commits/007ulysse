Ulysse

Très gros morceau.
Ton ProbabilityModelService est déjà très avancé, on est clairement sur un moteur de prono maison, pas un jouet.

Je te fais une analyse en trois points, puis je te propose des améliorations ciblées “Super Ulysse / Core Djedou Pronos”.

1️⃣ Ce que tu fais déjà très bien
Tu utilises un modèle Poisson raisonnable:

expected goals (homeLambda, awayLambda) basés sur:
goals for/against,
moyenne de ligue,
avantage domicile.
double somme sur 0–6 buts pour:
1X2,
over/under (1.5, 2.5, 3.5),
BTTS,
over 0.5 par équipe.
Tu blendes stats + cotes:

60% modèle stats, 40% cotes implicites → poisson_stats_odds_blend.
Tu renormalises 1X2 après mix → propre.
Tu ajoutes des couches métier:

ajustement forme récente (formToScore sur les 5 derniers résultats),
ajustement over/BTTS avec over25Rate / bttsRate des équipes,
augmentation de confidence si stats réelles dispo.
Tu construis des BetRecommendations propres:

probas, cotes, value %, confidence “high/medium/low”,
reasoning textuel.
Tu as une sortie formatée IA (formatPredictionsForAI) très exploitable:

TOP PICKS (confiance ≥ 0.7),
autres matchs,
note sur l’absence de garantie.
Bref : pour un moteur maison, c’est déjà carré.

2️⃣ Quelques points à sécuriser / améliorer
Je vais te pointer les points sensibles, puis te proposer des corrections / refines.

A. Forme : ordre des matchs et pondération
formToScore(form: string) lit les 5 premiers caractères du string.
Tu pars du principe que form[0] = plus récent.
Assure-toi que dans cachedTeamStats.formString, c’est bien le cas.
Si jamais la string stockée est dans l’autre sens (ancien → récent), ton poids est à l’envers.

Si tu veux être sûr, tu peux inverser si besoin ou documenter dans ulysse_core.md que formString est stocké plus récent en premier.

B. Poisson: borne 0–6 buts
Tu boucles 0–6 pour chaque équipe.
Dans la vraie vie, c’est largement suffisant, mais pour certains matchs ( ligues exotiques ), ça peut légèrement sous-estimer les extrêmes.
Tu peux soit te contenter de 0–6 (OK), soit monter à 0–7/8, mais avec coût CPU.

Honnêtement: pour ce que tu veux faire, 0–6 me va.

C. homeCleanSheet / awayCleanSheet
Ici:

homeCleanSheet: Math.round((1 - awayOver05Prob) * home.cleanSheetRate * 2 * 1000) / 10,
awayCleanSheet: Math.round((1 - homeOver05Prob) * away.cleanSheetRate * 2 * 1000) / 10,
Tu multiplies par 2 sans vraie justification.
C’est une espèce de boost “hardcodé”.

Je te suggère:

soit tu l’assumes dans ulysse_core.md (“Boost arbitraire pour calibrer d’après historique Djedou”),
soit tu enlèves le * 2 et tu ajustes cleanSheetRate directement dans tes stats.
Si tu veux rester “modèle propre”, je ferais:

homeCleanSheet: Math.round((1 - awayOver05Prob) * home.cleanSheetRate * 1000) / 10,
awayCleanSheet: Math.round((1 - homeOver05Prob) * away.cleanSheetRate * 1000) / 10,
Et tu calibres cleanSheetRate dans cachedTeamStats.

D. Confidence globale
Tu initialises:

let confidence = 0.6;
...
if (homeOdds && drawOdds && awayOdds) {
  ...
  confidence = 0.75;
}
...
if (homeStats && awayStats) {
  confidence += 0.1;
}
Résultat: 0.6 / 0.75 / 0.85 max.

Ensuite Math.min(0.95, confidence).

C’est propre, mais tu pourrais être un peu plus “explicite” dans l’utilisation:

confidence = confiance dans le modèle, pas dans chaque pari.
Tu pourrais ajouter un petit tag / niveau:
const modelConfidenceLevel =
  confidence >= 0.8 ? "high" : confidence >= 0.65 ? "medium" : "low";
Et l’injecter dans le format IA / UI, ce que tu fais déjà partiellement dans formatSinglePrediction (Confiance modèle: X%).

3️⃣ Propositions “Super Ulysse / Djedou Pronos Core”
Je te donne quelques upgrades concrets, qui respectent ta logique mais la renforcent.

3.1. Séparer la logique “value bet”
Tu calcules déjà valueRating.
Tu peux marquer explicitement les bets offrant vraie value, par exemple:

valueRating >= 10 → valueTier = "strong"
5 <= valueRating < 10 → "moderate"
< 5 → "none".
Et refléter ça dans BetRecommendation:

interface BetRecommendation {
  betType: string;
  prediction: string;
  probability: number;
  impliedOdds: number;
  bookmakerOdds?: number;
  valueRating: number;
  valueTier?: "none" | "moderate" | "strong";
  confidence: "high" | "medium" | "low";
  reasoning: string;
}
Et dans generateRecommendations:

let valueTier: "none" | "moderate" | "strong" = "none";
if (valueRating >= 10) valueTier = "strong";
else if (valueRating >= 5) valueTier = "moderate";

recommendations.push({
  ...,
  valueTier,
});
Ça te permet ensuite, côté UI/IA, de filtrer les “SPOTS DE VALUE” facilement.

3.2. Intégrer ton cache odds/matchs dans l’autonomousResearch / Verified Scraper
Tu as déjà cachedMatches, cachedOdds, cachedTeamStats, etc.
Ton probabilityModelService ne dépend que de ce cache : c’est parfait.

Ce que tu peux faire en plus:

Quand autonomousResearchService traite des questions sur les matchs du jour:
utiliser les résultats de ce modèle comme input dans le contexte, en plus des MARS/Verified.
Ou, dans le sens inverse:
quand Verified Scraper trouve des cotes / stats, tu peux remplir cachedTeamStats / cachedOdds pour nourrir ce modèle.
Tu as déjà tout le lien structurel, c’est juste une question d’orchestration.

3.3. Mode “safe” vs “aggressif” pour les recommandations
Tu pourrais vouloir deux modes:

safe:
ne garder que les bets:
probabilité > 55%,
valueRating > 5,
confiance modèle >= 0.7.
aggressif:
accepter plus de bets, y compris value faible mais avec cotes plus élevées.
Un paramètre simple dans generateRecommendations:

generateRecommendations(
  probs: MatchProbabilities,
  ...,
  mode: "safe" | "aggressive" = "safe"
)
Puis filtrer à la fin en fonction de ce mode.

4️⃣ Petits refactors qualitatifs
4.1. Factoriel & Poisson – protéger contre les overflow
Ton factorial monte vite si tu augmentes les bornes, mais comme tu es en 0–6, ça passe.
On peut laisser comme ça.

Si un jour tu veux être plus safe, tu peux:

soit passer par une table pré-calculée pour 0–10,
soit log-trick, mais c’est overkill pour ton usage.
4.2. Clarifier method
Tu mets:

method = "poisson_stats" ou "poisson_stats_odds_blend";
Ça peut être utile de garder ce method en base / logs pour analyser dans le temps:

est-ce que les matchs où on a des cotes + stats (blend) performent mieux que ceux où on n’a que les stats ?
Tu as déjà le champ, garde-le et exploite-le dans les analyses plus tard.

5️⃣ Conclusion
Ton moteur de prono est déjà :

statistiquement cohérent (Poisson + stats d’équipe),
corrigé par le marché (cotes),
enrichi par des indicateurs classiques (over/BTTS, clean sheets),
avec une sortie propre pour IA et UI.
Les améliorations que je te propose sont surtout :

clarifier quelques coefficients “magiques” (genre