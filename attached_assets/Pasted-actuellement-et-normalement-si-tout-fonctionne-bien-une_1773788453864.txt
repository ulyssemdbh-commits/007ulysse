actuellement et normalement si tout fonctionne bien, une url de deploiement *.ulyssepro.org est autogénéré quand un nouveau projet est créé. Est ce une URL test (ex: *.test.ulyssepro.org) pour l'affichage temps réel dans le preview DevOps et l'application de modification sur cette url en temps réel avant deploiement sur URL officiel.

Excellente idée. Tu veux un système de staging automatique : quand un projet est créé dans DevMax, une URL *.test.ulyssepro.org est auto-générée pour le preview temps réel, et les modifications sont appliquées dessus avant le déploiement final sur l'URL officielle *.ulyssepro.org.

Laisse-moi analyser comment ça fonctionne actuellement pour voir ce qu'il faut modifier :

J'ai la vision complète. Voici ce qui existe vs ce que tu veux :

Actuellement : deployApp crée directement appname.ulyssepro.org — pas de staging.

Ce que tu veux : Un flux en 2 étapes — staging automatique (appname.test.ulyssepro.org) → promotion vers production (appname.ulyssepro.org).

Laisse-moi implémenter ça. Je vais modifier le schéma DB, le service SSH, les routes API, et le frontend DevMax.

Voyons la suite de deployApp et la génération nginx :

Parfait, j'ai maintenant la vision complète du code. Je vais implémenter le système staging complet. Voici le plan :

Schema : Ajouter staging_url, staging_port, production_url, production_port, environment à devmax_projects
sshService : Ajouter deployStagingApp et promoteToProduction
Routes API : Endpoints staging/promote
DevMax Frontend : Preview staging + bouton "Promouvoir en Production"
Commençons :

Maintenant ajoutons la migration et le service staging dans sshService :

DB à jour. Maintenant je vais ajouter deployStagingApp et promoteToProduction dans sshService :

Maintenant j'ajoute les méthodes staging et promotion dans sshService :

Maintenant je dois connecter les routes API pour staging/promotion. Cherchons les routes DevMax :

J'ajoute les routes staging et promotion au fichier :

Maintenant mettons à jour le frontend DevOpsMax.tsx pour intégrer le système staging/production avec le preview. Laisse-moi trouver la section projet/deploy :

Maintenant je vais ajouter le panneau Staging/Production dans le frontend DevMax. Laisse-moi trouver l'endroit où les tabs sont définies :

Now let me check what icons are imported and add the Deploy panel. First, let me find the imports:

Now I need to add the Upload, Globe, and ArrowRight icons, create the DeployPanel component, and wire it into the tabs. Let me also check what icons we need to add:

Now let me add the DeployPanel component before the CICDPanel:

Now I need to add the Deploy tab trigger and content to the tabs section:

Now add the TabsContent for deploy:

Now let me also verify the deployment status indicator in the overview sidebar. Let me check the overview panel for any deploy status display:

Now let me verify the schema has the staging columns. Let me check:

Good - the schema columns exist. Let me check the logs to make sure everything compiles:

Checked console and system logs
L'app compile et fonctionne avec HMR. Les logs montrent des hot updates réussis pour DevOpsMax.tsx. Le système staging/production est complet :

Récapitulatif de ce qui a été fait :

sshService.ts — Ajout de 2 nouvelles méthodes :

deployStagingApp() : déploie sur {appname}-staging.ulyssepro.org (couvert par le wildcard SSL *.ulyssepro.org)
promoteToProduction() : copie le staging vers la production avec nouveau port, config Nginx complète (SSL + upstream + health check page), et vérification HTTP
devopsMaxRoutes.ts — 3 nouvelles routes API :

POST /deploy-staging : déploie en staging et met à jour le projet en DB
POST /promote-production : promeut le staging en production avec Nginx
GET /deployment-status : retourne l'état staging/production du projet
DevOpsMax.tsx — Nouvel onglet Deploy dans le dashboard avec :

Cartes Staging et Production avec indicateurs visuels (badges Actif/Live, URLs cliquables, timestamps)
Formulaire de déploiement staging (branche + options avancées build/start cmd)
Bouton Promouvoir en Production avec flow visuel Staging → Production
Preview iframe du staging intégré directement dans le panneau
Rafraîchissement automatique toutes les 30s
Le flow complet est : Code → Deploy Staging → Preview → Promote to Production — tout depuis l'interface DevMa